<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartrecruit.referral.repository.ReferralProgramMapper">

    <select id="selectEnabledPrograms" resultType="com.smartrecruit.referral.entity.ReferralProgram">
        SELECT id, title, description, bonus_amount, bonus_structure,
               start_date, end_date, eligible_dept_ids, status,
               create_time, update_time, create_user_id, create_by,
               update_user_id, update_by
        FROM ref_program
        WHERE status = 1 AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectPageWithQuery" resultType="com.smartrecruit.referral.entity.ReferralProgram">
        SELECT id, title, description, bonus_amount, bonus_structure,
               start_date, end_date, eligible_dept_ids, status,
               create_time, update_time, create_user_id, create_by,
               update_user_id, update_by
        FROM ref_program
        <where>
            deleted = 0
            <if test="query.keyword != null and query.keyword != ''">
                AND (title LIKE CONCAT('%', #{query.keyword}, '%')
                     OR description LIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
            <if test="query.status != null">
                AND status = #{query.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
