<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartrecruit.offer.repository.OfferApprovalMapper">

    <select id="selectByOfferId" resultType="com.smartrecruit.offer.entity.OfferApproval">
        SELECT * FROM rec_offer_approval
        WHERE offer_id = #{offerId}
        ORDER BY approval_level ASC, create_time DESC
    </select>

    <select id="selectLatestByOfferId" resultType="com.smartrecruit.offer.entity.OfferApproval">
        SELECT * FROM rec_offer_approval
        WHERE offer_id = #{offerId}
        ORDER BY create_time DESC
        LIMIT 1
    </select>

</mapper>
