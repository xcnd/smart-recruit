<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartrecruit.aiengine.repository.AiAgentTaskMapper">

    <select id="selectRecentTasks" resultType="com.smartrecruit.aiengine.entity.AiAgentTask">
        SELECT id, agent_name, task_type, priority, input_data, output_data,
               status, started_at, completed_at, duration_ms, error_message, created_at
        FROM ai_agent_task
        ORDER BY (status IN (0, 1)) DESC, created_at DESC
        LIMIT 50
    </select>

    <select id="selectByAgentName" resultType="com.smartrecruit.aiengine.entity.AiAgentTask">
        SELECT id, agent_name, task_type, priority, input_data, output_data,
               status, started_at, completed_at, duration_ms, error_message, created_at
        FROM ai_agent_task
        WHERE agent_name = #{agentName}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
